target/
dist/
**/*.rs.bk
/im-rc

# Nix / devenv
.devenv*
devenv.local.nix
result

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# Virtual environments (should not exist, but guard against it)
.venv/
