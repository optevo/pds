target/
dist/
**/*.rs.bk
/im-rc

# Nix / devenv
.devenv*
devenv.local.nix
result

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# Virtual environments (should not exist, but guard against it)
.venv/

# Profiling artefacts
*.json.gz
dhat-heap.json

# Local rustdoc output
rustdocs/

# Benchmark output files — raw criterion output, not committed (summaries go in docs/baselines.md)
docs/bench_*.txt
docs/bench_latest.txt

# Editor directories
.vscode/
.idea/
